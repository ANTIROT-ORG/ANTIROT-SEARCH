# Antirot Dependencies — classical IR only, no neural models
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0

# Fast tier (DuckDB local / MotherDuck cloud when MOTHERDUCK_TOKEN is set) + cache client
duckdb>=1.0.0
redis>=5.0.0

# HuggingFace (raw-layer archive)
huggingface-hub>=0.20.0

# Database
sqlalchemy>=2.0.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
jinja2>=3.1.0
